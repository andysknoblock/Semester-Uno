
public class HelloWorld 
{
     public static void main(String[] args) 
     {
       float a,b,c,d,max=100000;
       b=float()(Math.random ()*max);
       b=float()(Math.random ()*max);
       c=float()(Math.random ()*max);
       d5P[90I8=float()(Math.random ()*max);

       
     }
}